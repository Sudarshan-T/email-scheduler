export enum EMAIL {
  SUBJECT = 'Test email subject',
  CONTENT = 'Test email content',
}

export enum EMAIL_STATUS {
  SUCCESS = 'Success',
  FAILED = 'Failed',
}

// Comma separated values
export const TO_MAIL_ID = '';
